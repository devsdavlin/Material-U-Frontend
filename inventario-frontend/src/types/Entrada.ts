export interface Entrada {
  id: string;
  fecha: string;
  codigoMig: string;
  materialId: string;
  descripcion: string;
  cantidad: number;
  unidadMedida: string;
  sedeId: string;
  registradoPor: string;
}